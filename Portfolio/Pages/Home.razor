@page "/"
@inject IJSRuntime JS

<canvas id="globeCanvas"></canvas>

@code {
    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            await JS.InvokeVoidAsync("initializeGlobe", "globeCanvas");
        }
    }
}